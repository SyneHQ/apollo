{
  "name": "synehq-analytics",
  "version": "1.0.0",
  "description": "Analytics job container for SyneHQ Apollo",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "redis": "^4.6.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.10.0"
  }
}
